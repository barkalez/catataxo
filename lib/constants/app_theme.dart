import 'package:flutter/material.dart';

ThemeData getAppTheme() {
  return ThemeData(
    primarySwatch: Colors.blue,
    // Puedes añadir más personalizaciones aquí, como:
    // scaffoldBackgroundColor: Colors.grey[100],
    // textTheme: TextTheme(...),
  );
}
name: catataxo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.0
  firebase_auth: ^5.5.0
  cloud_firestore: ^5.6.4
  logger: ^2.5.0
  choice: ^2.3.2
  animated_tree_view: ^2.3.0
  shared_preferences: ^2.5.2
  flutter_form_builder: ^9.7.0
  form_builder_extra_fields: ^11.1.0
  google_sign_in: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/google_logo.png
  uses-material-design: true # Corregida la indentación, debe estar al mismo nivel que "assets"